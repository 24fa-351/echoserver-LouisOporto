# 351-redirect


# Requirements
Create a server that communicates with incoming clients that want to echo and communicate

- The server needs to broadcast message back to client socket.
- Client receive back message they sent (telnet)
- Clients can send messages to the server
- Multiple users can join the server (TODO)
- Server has argument to select port to listen to. `<port>`

